fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
sqlalchemy>=2.0
pydantic>=2.6
pdfplumber>=0.11
python-docx>=1.1

# Job-URL scraping
httpx>=0.27
beautifulsoup4>=4.12
trafilatura>=1.12  # optional but improves body extraction; remove if unwanted

# Semantic search (Phase 3). Optional — without these the API stays up but
# /api/search/semantic returns 503 and matching falls back to BoW similarity.
sentence-transformers>=2.7
faiss-cpu>=1.8
numpy>=1.26
